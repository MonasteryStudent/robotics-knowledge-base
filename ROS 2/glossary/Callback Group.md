## Definition

A callback group organizes callbacks within a [[Node]] and specifies whether callbacks in that group may run in parallel (reentrant) or must run one at a time (mutually exclusive). A callback group does not itself create threads.