## Definition

An executor is a ROS 2 component that waits for events and runs the corresponding [[Callback Function|callbacks]] for one or more [[Node|nodes]]. It can use one or multiple threads to process them.