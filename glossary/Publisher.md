
## Definition

A publisher is a ROS 2 entity that sends messages to a [[Topic]].

## Purpose

A publisher allows a [[Node]] to share data with other nodes by publishing messages to a topic.

## Example

- A node publishing velocity commands to the `/turtle1/cmd_vel` topic.