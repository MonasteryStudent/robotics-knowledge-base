## Definition

A subscriber is a ROS 2 entity that receives messages from a [[Topic]].

## Purpose

A subscriber allows a [[Node]] to receive data published by other nodes through a topic.

## Example

- A node subscribing to the `/number` topic to receive integer values.