## Definition

A ROS 2 interface specifies the structure of data exchanged through [[Topic|topics]], [[Service|services]], or [[Action|actions]]. Message (`.msg`), service (`.srv`), and action (`.action`) interfaces define the data needed for their respective communication mechanisms.